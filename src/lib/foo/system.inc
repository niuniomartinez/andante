(* Template to implement the core system.

   This file is included by unit andante.pas. *)

(* Initializes the system. *)
  function _InitSystem: Boolean;
  begin
    anError := anNotImplemented;
    _InitSystem := False
  end;



(* Closes the system. *)
  procedure _CloseSystem;
  begin
  end;
